# Environment Variables Configuration
# Copy this file to .env.local and configure for your environment

# ===== Backend API Configuration =====

# BACKEND_URL: Where Next.js should proxy API requests (server-side)
# Used by next.config.js rewrites to forward /api/* requests
#
# Development (WSL): Use WSL network IP
# BACKEND_URL=http://172.29.144.1:8000
#
# Development (Native): Use localhost
# BACKEND_URL=http://localhost:8000
#
# Production (Same VM): Use localhost (default)
# BACKEND_URL=http://localhost:8000
#
# Production (Separate servers): Use backend domain
# BACKEND_URL=https://api.yourdomain.com

# Default if not set: http://localhost:8000
BACKEND_URL=http://localhost:8000

# ===== Advanced: Direct Browser Calls (Optional) =====

# NEXT_PUBLIC_API_URL: Makes browser call backend directly (bypasses proxy)
# Only use this if you need client-side direct API calls
# Requires CORS configuration on backend
#
# Leave commented to use the proxy (recommended)
# NEXT_PUBLIC_API_URL=http://172.29.144.1:8000
