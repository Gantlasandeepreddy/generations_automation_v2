# Backend URL for Next.js server-side proxy
# This tells Next.js where to forward /api/* requests
#
# For WSL development: use the WSL network IP
BACKEND_URL=http://172.29.144.1:8000

# Optional: Override to use direct browser calls instead of proxy
# Leave commented to use the proxy (recommended)
# NEXT_PUBLIC_API_URL=http://172.29.144.1:8000
